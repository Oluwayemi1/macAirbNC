const data = require("../db/utils");
const {
  formattedPropertyTypes,
  formattedUsers,
  reformattedUsers,
  formattedProperties,
  reformattedProperties,
  usersWithReviews,
  formattedReviews,
} = data;
